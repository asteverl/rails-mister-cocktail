<div class="container">
  <p>Enter a dose for the cocktail <%= @cocktail.name %></p>

  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description %>
    <%= f.submit "Add a dose", class: "btn btn-primary" %>
  <% end %>
</div>
